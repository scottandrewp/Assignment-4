@{
    ViewData["Title"] = "Home Page";
}

@model IEnumerable<string>


<div class="text-center">
    <ul>
        @foreach (string i in Model)
        {
            <li>@Html.Raw(i)</li>
           
        }
    </ul>
</div>
